apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parse-server.fullname" . }}-test-connection"
  labels:
{{ include "parse-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "parse-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
